Q:出于何种目的？
A:对项目中开发的可能公用的ＪＳ代码进行整理，保存，便于在后来的项目开发中直接调用，减少开发成本，提高开发效率。
这要求ＪＳ代码封装良好，有一定的可重性性
解决什么问题？
加快...速度，提高...效率，方便...维护，节约...时间成本，改变..不好的状况.
----------------------------------------

Q:建库思想，使用原则？
A:

“不能解决所有问题。按我们规定的方法去做！”
追求长用不衰，与时俱进。更新成长能力最最重要。
收集、整理
----------------------------------------
Q:js代码使用规范？
A:

----------------------------------------
Q:基于公共库jQuery？
A:jQuery插件是库的一部分

----------------------------------------
Q:一个文件？多个文件？
A:相同的归类，文件尽量少

----------------------------------------
Q:库的兼容？将jQuery作为一种接口的实现．用到的方法写成接口．
A:暂不考虑此问题

----------------------------------------


================================================================================


====================================

公用组件
------------------------------------

popup（弹出层，包括数据交互模型）
calendar（日历）
iselect（自定义select）
draggable（使页面元素可拖动）
resizable（使页面元素可改变大小）
sortTable（可排序的表格）
softKeyboard（软键盘）


editor（编辑器）

级联菜单
右键菜单

grayTips（输入框的灰色提示，点击消失）




====================================

表单验证
------------------------------------

ValidateMethods（表单验证框架）
输入限制(numberonly、wordonly、moneyonly、allowpoint)
checkForm


====================================

TOOLS
------------------------------------

trim
copyToClipboard（复制到剪贴板）
addFavorite（添加收藏）
setHome（设为首页）
queryString（取得ＵＲＬ的参数）
cookie(get,set,delete)（cookie的读、写、删）
byteLength（计算字符串的字节大小，一个中文算两个字节）





====================================

More..
------------------------------------
matrix （密保卡录入）
getKeyType
getPos
tree_node



====================================

DEVTOOLS
------------------------------------

printObject
printString


====================================

Model　各种解决方案模型
------------------------------------

游戏商品选择与选择后数据变动